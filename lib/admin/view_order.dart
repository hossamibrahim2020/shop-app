import 'package:flutter/material.dart';

class ViewOrders extends StatelessWidget {
  static const String route = '/ViewOrders';
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}